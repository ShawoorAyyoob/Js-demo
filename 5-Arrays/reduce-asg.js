const numbers = [3, 5, 7, 9];
let sum = numbers.reduce((total, n) => total + n);
console.log(` Sum = ${sum}`);

let pro = numbers.reduce((mu, n) => mu * n);
console.log(` Pro = ${pro}`);

let conCat = numbers.reduce((mu, n) => mu * n);
